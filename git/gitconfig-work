# Work-specific git overrides — included conditionally from gitconfig via [includeIf].
# Do not use this file as a standalone ~/.gitconfig.

[user]
    email = justin.puah@hollard.com.au

[core]
    excludesFile = ~/.gitignore-work

[diff]
    tool = delta
