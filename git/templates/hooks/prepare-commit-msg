#!/bin/sh
DIR="$(cd "$(dirname "$0")" && pwd)"
if command -v pwsh >/dev/null 2>&1; then
    exec pwsh -NoProfile -File "$DIR/prepare-commit-msg.ps1" "$@"
else
    exec sh "$DIR/prepare-commit-msg.sh" "$@"
fi
