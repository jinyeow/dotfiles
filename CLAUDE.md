# Claude Code project adapter

@AGENTS.md

The repository guidance above is canonical and is shared with Codex CLI and Pi through
this root `AGENTS.md`. This file contains only Claude Code-specific behavior.

## Claude Code behavior

- Use the repository's Claude Code hooks and skills when they apply; do not reproduce their guidance here.
- Keep Claude-specific workflow, tool-use, and orchestration behavior in the global Claude adapter (`claude/CLAUDE.md`), not in the shared project instructions.
