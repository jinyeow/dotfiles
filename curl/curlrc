# Follow redirects
-L

# Automatically set the previous URL as referer when following a redirect
referer = ";auto"

# Abort if connection takes longer than 30 seconds (does not limit transfer time)
connect-timeout = 30
